<div class="container">
  <div class="row">
    <h4 class="mt-5">お気に入り一覧</h4>
  </div>
  <div class="row">
    <% @favorite_posts.each do |post| %>
      <%= link_to post_path(post.id) do %>
        <%= image_tag(post.image) %>
      <% end %>
    <% end %>
  </div>
</div>
