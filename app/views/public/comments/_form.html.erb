<%= form_with(model:[post, comment], local: true) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントを投稿", class: "w-100" %>
  <%= f.submit "送信する", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>